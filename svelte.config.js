// bladocs/svelte.config.js
/** @type {import('@sveltejs/kit').Config} */
const config = {
	// This file exists only to tell the VS Code / Neovim extension
	// that this workspace root contains Svelte projects.
};

export default config;
