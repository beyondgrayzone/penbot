# Input

**Import:** `import { Input, Label } from "@bladocs/core";`

## Usage

```svelte
<Label for="name">Your name</Label>
<Input id="name" name="name" placeholder="John Doe" />
```

Works with standard HTML input attributes (`id`, `name`, `placeholder`, `disabled`, etc.).
